server:
    port: 8888

spring:
    profiles:
        active: dev
    cloud:
        config:
            server:
                git:
                    uri: "https://github.com/FlavioTroja/EQ"
                    searchPaths: "configrepo"
                    clone-on-start: true
